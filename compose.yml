services:
  ros2-jazzy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ros2-jazzy
    hostname: ros2-jazzy
    volumes:
      - ./ros2_ws:/root/ros2_ws
    working_dir: /root/ros2_ws
    tty: true